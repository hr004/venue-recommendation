## 📋 Submission Checklist

- [x] Working API endpoint (POST /api/{domain}/recommend)
- [x] 3+ agents with clear roles (Capacity, Amenity, Location, etc.)
- [x] RAG system using historical events/expansions/relationships
- [x] README with setup instructions (< 5 min) (simply run `make run` -> go to api docs and execute index endpoint and then recommend endpoint)
- [x] .env.example with required API keys (using .env file instead of creating a new one from the example)
- [x] Response format matches assignment specification
